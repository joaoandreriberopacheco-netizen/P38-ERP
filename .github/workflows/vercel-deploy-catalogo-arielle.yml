# Deploy estático — catálogo Arielle (Carmelo Fior).

name: Vercel Deploy — Catálogo Arielle

concurrency:
  group: vercel-deploy-catalogo-arielle
  cancel-in-progress: false

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'deploy/catalogo-arielle/**'
      - '.github/workflows/vercel-deploy-catalogo-arielle.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Sanitizar secrets Vercel
        id: vercel_secrets
        shell: bash
        env:
          RAW_VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          RAW_VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          RAW_VERCEL_PROJECT_ID: ${{ secrets.VERCEL_CATALOGO_ARIELLE_PROJECT_ID }}
        run: |
          set -e
          trim() { printf '%s' "$1" | tr -d '\n\r'; }
          token=$(trim "$RAW_VERCEL_TOKEN")
          org=$(trim "$RAW_VERCEL_ORG_ID")
          project=$(trim "$RAW_VERCEL_PROJECT_ID")
          if [ -z "$token" ] || [ -z "$org" ] || [ -z "$project" ]; then
            echo "::warning::Secrets Vercel do catálogo Arielle em falta — deploy ignorado."
            echo "Configure VERCEL_CATALOGO_ARIELLE_PROJECT_ID (ver deploy/catalogo-arielle/README.md)."
            echo "ready=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          {
            echo "VERCEL_TOKEN=$token"
            echo "VERCEL_ORG_ID=$org"
            echo "VERCEL_PROJECT_ID=$project"
          } >> "$GITHUB_ENV"
          echo "ready=true" >> "$GITHUB_OUTPUT"

      - name: Validar index.html
        run: |
          set -e
          if [ ! -f deploy/catalogo-arielle/index.html ]; then
            echo "::error::deploy/catalogo-arielle/index.html em falta. Rode npm run catalogo:publicar-arielle"
            exit 1
          fi
          bytes=$(wc -c < deploy/catalogo-arielle/index.html)
          if [ "$bytes" -lt 100000 ]; then
            echo "::error::index.html demasiado pequeno ($bytes bytes)."
            exit 1
          fi
          echo "OK: index.html ($bytes bytes)"

      - name: Vercel deploy (catálogo Arielle)
        if: steps.vercel_secrets.outputs.ready == 'true'
        working-directory: deploy/catalogo-arielle
        run: |
          url=$(npx --yes vercel@latest deploy --prod --yes --token "$VERCEL_TOKEN" --project "$VERCEL_PROJECT_ID")
          echo "Deploy URL: $url"
