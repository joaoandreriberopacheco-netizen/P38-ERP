#env
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.vercel
node_modules

# Exportações locais (dados do catálogo — não versionar)
artifacts/
dist
dist-ssr
.next
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Export local da fila Flare (pode conter dados sensíveis)
docs/flare-export/flare-pending.json

# Relatórios de auditoria com dados financeiros reais
docs/audit/

# Clone só-leitura do A29 para comparação (ver docs/reference-a29-erp/README.md)
docs/reference-a29-erp/checkout/

# Cache local de scripts (inferência, fragmentos gerados)
scripts/.cache/

# Espelho legacy gerado localmente (npm run mirror:pack-legacy) — não versionar
mirror/legacy/

# Índice do overlay visual (feature removida)
src/generated/catalog-overlay-index.json

# Ficheiro mestre de chaves P38 (João cola tudo aqui no Cloud — NUNCA commitar)
secrets/p38-chaves.txt
secrets/p38-chaves.local.txt

# Artefactos temporários de agentes / sessões (nunca versionar)
_mcp*
.cursor-tmp-mcp-*

.env
base44/.app.jsonc
supabase/.temp/
