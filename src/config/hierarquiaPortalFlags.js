/**
 * Portal de preview da hierarquia (Categoria → LINHA → Produto compra → SKU).
 * Não altera cadastro nem SMART SUPPLY real — só simulação em memória.
 */
export const HIERARQUIA_PORTAL_ENABLED = true;
