/**
 * SMART SUPPLY — módulo canónico de reposição.
 *
 * A rota continua `SugestoesCompra` (URL estável); o produto junta
 * sugestão de compra + cotação ao fornecedor num só ecrã.
 *
 * Portal Hierarquia = preview da visão por LINHA (piloto cerâmica) antes
 * de convergir totalmente neste módulo.
 */
export const SMART_SUPPLY_PAGE = 'SugestoesCompra';

export const SMART_SUPPLY_TITLE = 'SMART SUPPLY';

export const SMART_SUPPLY_SUBTITLE =
  'Sugestão de reposição e cotação ao fornecedor — giro, ponto futuro e pedido';

/** Nome curto para menus e permissões. */
export const SMART_SUPPLY_MENU_LABEL = 'SMART SUPPLY';

/** Portal = laboratório/preview; produção = SMART_SUPPLY_PAGE. */
export const SMART_SUPPLY_PORTAL_PREVIEW_LABEL = 'Preview SMART SUPPLY (piloto)';
